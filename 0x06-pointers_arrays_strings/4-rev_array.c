#include "main.h"

/**
 * reverse_array - function to reverse an array
 * @a: array in question
 * @n: no. of array elements
 *
 * Return: void
 */
void reverse_array(int *a, int n)
{
	int i, Newarray;

	for (i = n - 1; i < n; i--)
	{
		Newarray = *(a + i);
	}
}
