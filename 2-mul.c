#include "main.h"

/**
 * mul - function that multiplies two integers
 * @a: first interger
 * @b: second integer
 * Return: returns the multi value
 */
int mul(int a, int b)
{
	int multi = a * b;

	return (multi);
}
