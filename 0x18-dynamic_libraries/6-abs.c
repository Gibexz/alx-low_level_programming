#include "main.h"

/**
 * _abs - Number to return it Absoulute valur
 * @a: Int value to be checked
 * Return: Absolute value
 */
int _abs(int a)
{
	if (a < 0)
	{
		a = -a;

		return (a);
	}
	else
	{
		return (a);
	}
}
