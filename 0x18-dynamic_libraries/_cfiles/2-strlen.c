#include <string.h>
#include "main.h"

/**
 * _strlen - function to return a string lenght
 * @s: string to be checked
 *
 * Return: String lenght
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
